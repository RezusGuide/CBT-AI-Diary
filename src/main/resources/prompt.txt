Push the entire project to GitHub branch "final".

Repository URL: https://github.com/RezusGuide/CBT-AI-Diary.git
Target branch: final

Steps:
1. Check current git status: git status
2. Check existing remotes: git remote -v
3. If remote "origin" already points to the correct repo, skip adding it.
   If not: git remote add origin https://github.com/RezusGuide/CBT-AI-Diary.git
4. Stage ALL files including untouched ones: git add -A
5. Commit: git commit -m "feat: full project — design reform + bug fixes (Part 1 & 2)"
6. Push to final branch: git push origin HEAD:final
   If the branch does not exist yet on remote, use: git push -u origin HEAD:final
7. If push is rejected due to diverged history, do NOT force push without telling me first.
   Instead, run: git pull origin final --rebase
   then push again.

Do NOT delete or modify any files during this process.
Do NOT push to main or master — only to the "final" branch.